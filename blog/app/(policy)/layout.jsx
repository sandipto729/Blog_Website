export const metadata = {
  title: "Policies - BlogSpace", 
  description: "Read our privacy policy, terms of service, and cookie policy for BlogSpace.",
  keywords: ["privacy", "terms", "cookies", "policy", "legal", "BlogSpace"],
};

export default function PolicyLayout({ children }) {
  return children;
}