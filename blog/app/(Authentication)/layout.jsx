export const metadata = {
  title: "Authentication - BlogSpace",
  description: "Login or signup to join the BlogSpace community and start sharing your stories.",
  keywords: ["login", "signup", "authentication", "BlogSpace", "join community"],
};

export default function AuthLayout({ children }) {
  return children;
}